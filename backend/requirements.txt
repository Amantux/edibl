Flask>=3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Cors>=5.0.0
Flask-Limiter>=3.8.0
SQLAlchemy==2.0.32
# Schema migrations (SQLite + Postgres). Replaces the old create_all/_ensure_columns.
alembic>=1.13
# Postgres driver (optional backend). psycopg 3 bundles libpq via [binary];
# unused when EDIBL_DATABASE_URL is blank (SQLite default).
psycopg[binary]>=3.2
PyJWT>=2.10.1
passlib==1.7.4
# passlib 1.7.4 reads bcrypt.__about__ which bcrypt>=4.1 removed; pin below that.
bcrypt==4.0.1
python-dotenv>=1.1.0
gunicorn>=23.0.0
# Edibl MCP server (AI tooling for myMeal / Home Assistant)
mcp>=1.9.0
uvicorn>=0.29.0
httpx>=0.27.0
